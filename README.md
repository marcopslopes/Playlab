
  # Update attached markdown

  This is a code bundle for Update attached markdown. The original project is available at https://www.figma.com/design/G0O1RmrCTYjwR0CAibipfZ/Update-attached-markdown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  